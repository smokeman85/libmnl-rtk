#lang setup/infotab

(define name "libmnl-rkt")
(define version "0.11")
(define blurb
  (list "FFI for libmnl"))
(define collection 'multi)
(define primary-file "libmnl-rkt.rkt")
(define categories '(io netlink linux))
